# =============== Database Configuration ===============
spring.datasource.url=jdbc:mysql://localhost:3306/tp_music?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=LTY;
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =============== MinIO Configuration ===============
minio.endpoint=http://localhost:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket-name=user01
minio.mv-bucket=mv-storage
# Dedicated bucket for MV storage

# =============== File Upload Configuration ===============
# Max size for single file
#TODO: ???????????mv????????????
spring.servlet.multipart.max-file-size=100MB
# Max size for single request
spring.servlet.multipart.max-request-size=100MB
# Special limit for MV files

# =============== Development Environment Settings ===============
# Show SQL logs
logging.level.com.example.yin.mapper=DEBUG
# CORS allowed origins
